diff a/src/plugin/controller.ts b/src/plugin/controller.ts	(rejected hunks)
@@ -138 +138,3 @@ function lint(
-        errors = errors.concat(lint(node.children, libraries, currentFrameId, savedTokens));
+        errors = errors.concat(
+          lint(node.children, libraries, currentFrameId, savedTokens)
+        );
@@ -200 +202,5 @@ function determineTypeWithFrame(
-function lintComponentRules(node: any, libraries: any[], savedTokens: any[] = []): any[] {
+function lintComponentRules(
+  node: any,
+  libraries: any[],
+  savedTokens: any[] = []
+): any[] {
@@ -212 +218,5 @@ function lintComponentRules(node: any, libraries: any[], savedTokens: any[] = []
-function lintLineRules(node: any, libraries: any[], savedTokens: any[] = []): any[] {
+function lintLineRules(
+  node: any,
+  libraries: any[],
+  savedTokens: any[] = []
+): any[] {
@@ -222 +232,5 @@ function lintLineRules(node: any, libraries: any[], savedTokens: any[] = []): an
-function lintFrameRules(node: any, libraries: any[], savedTokens: any[] = []): any[] {
+function lintFrameRules(
+  node: any,
+  libraries: any[],
+  savedTokens: any[] = []
+): any[] {
@@ -234 +248,5 @@ function lintFrameRules(node: any, libraries: any[], savedTokens: any[] = []): a
-function lintTextRules(node: any, libraries: any[], savedTokens: any[] = []): any[] {
+function lintTextRules(
+  node: any,
+  libraries: any[],
+  savedTokens: any[] = []
+): any[] {
@@ -247 +265,5 @@ function lintTextRules(node: any, libraries: any[], savedTokens: any[] = []): an
-function lintRectangleRules(node: any, libraries: any[], savedTokens: any[] = []): any[] {
+function lintRectangleRules(
+  node: any,
+  libraries: any[],
+  savedTokens: any[] = []
+): any[] {
@@ -260 +282,5 @@ function lintRectangleRules(node: any, libraries: any[], savedTokens: any[] = []
-function lintShapeRules(node: any, libraries: any[], savedTokens: any[] = []): any[] {
+function lintShapeRules(
+  node: any,
+  libraries: any[],
+  savedTokens: any[] = []
+): any[] {
@@ -404 +430 @@ figma.ui.onmessage = async (msg: UIMessage) => {
-    if (msg.type === 'load-saved-tokens') {
+    if (msg.type === "load-saved-tokens") {
@@ -407 +433 @@ figma.ui.onmessage = async (msg: UIMessage) => {
-        type: 'saved-tokens-loaded',
+        type: "saved-tokens-loaded",
@@ -416 +442 @@ figma.ui.onmessage = async (msg: UIMessage) => {
-    if (msg.type === 'save-design-tokens' && msg.tokens) {
+    if (msg.type === "save-design-tokens" && msg.tokens) {
@@ -419 +445 @@ figma.ui.onmessage = async (msg: UIMessage) => {
-        type: 'design-tokens-saved',
+        type: "design-tokens-saved",
@@ -437 +463,6 @@ figma.ui.onmessage = async (msg: UIMessage) => {
-          const lintResults = lint(allNodes, effectiveLibraries, null, msg.savedTokens || []);
+          const lintResults = lint(
+            allNodes,
+            effectiveLibraries,
+            null,
+            msg.savedTokens || []
+          );
@@ -906 +937,6 @@ figma.ui.onmessage = async (msg: UIMessage) => {
-        const lintResults = lint(allNodes, effectiveLibraries, null, msg.savedTokens || []);
+        const lintResults = lint(
+          allNodes,
+          effectiveLibraries,
+          null,
+          msg.savedTokens || []
+        );
@@ -1066 +1102,2 @@ async function loadSavedTokens() {
-    const savedFiles = (await figma.clientStorage.getAsync('savedTokenFiles')) || [];
+    const savedFiles =
+      (await figma.clientStorage.getAsync("savedTokenFiles")) || [];
@@ -1068 +1105 @@ async function loadSavedTokens() {
-    
+
@@ -1071,2 +1108,3 @@ async function loadSavedTokens() {
-    for (const file of (savedFiles as SavedTokenFile[]).sort((a, b) => 
-      new Date(b.timestamp).getTime() - new Date(a.timestamp).getTime()
+    for (const file of (savedFiles as SavedTokenFile[]).sort(
+      (a, b) =>
+        new Date(b.timestamp).getTime() - new Date(a.timestamp).getTime()
@@ -1086 +1124 @@ async function loadSavedTokens() {
-    
+
@@ -1092 +1130 @@ async function loadSavedTokens() {
-    console.error('Erro ao carregar tokens salvos:', error);
+    console.error("Erro ao carregar tokens salvos:", error);
@@ -1095 +1133,4 @@ async function loadSavedTokens() {
-      message: error instanceof Error ? error.message : 'Falha ao carregar os tokens salvos'
+      message:
+        error instanceof Error
+          ? error.message
+          : "Falha ao carregar os tokens salvos"
@@ -1104 +1145 @@ async function saveDesignTokens(tokens: any) {
-    const timestamp = new Date().toISOString().replace(/[:.]/g, '-')
+    const timestamp = new Date().toISOString().replace(/[:.]/g, "-");
@@ -1106 +1147 @@ async function saveDesignTokens(tokens: any) {
-    
+
@@ -1108,2 +1149,5 @@ async function saveDesignTokens(tokens: any) {
-    await figma.clientStorage.setAsync(filename, JSON.stringify(tokens, null, 2));
-    
+    await figma.clientStorage.setAsync(
+      filename,
+      JSON.stringify(tokens, null, 2)
+    );
+
@@ -1111,2 +1155,3 @@ async function saveDesignTokens(tokens: any) {
-    const savedFiles = (await figma.clientStorage.getAsync('savedTokenFiles')) || [];
-    
+    const savedFiles =
+      (await figma.clientStorage.getAsync("savedTokenFiles")) || [];
+
@@ -1119 +1164 @@ async function saveDesignTokens(tokens: any) {
-    
+
@@ -1121,2 +1166,2 @@ async function saveDesignTokens(tokens: any) {
-    await figma.clientStorage.setAsync('savedTokenFiles', savedFiles);
-    
+    await figma.clientStorage.setAsync("savedTokenFiles", savedFiles);
+
@@ -1124 +1169 @@ async function saveDesignTokens(tokens: any) {
-    
+
@@ -1127,4 +1172,4 @@ async function saveDesignTokens(tokens: any) {
-    
-    return { 
-      success: true, 
-      message: 'Design tokens salvos com sucesso',
+
+    return {
+      success: true,
+      message: "Design tokens salvos com sucesso",
@@ -1136,4 +1181,7 @@ async function saveDesignTokens(tokens: any) {
-    console.error('Erro ao salvar design tokens:', error);
-    return { 
-      success: false, 
-      message: error instanceof Error ? error.message : 'Falha ao salvar os tokens de design' 
+    console.error("Erro ao salvar design tokens:", error);
+    return {
+      success: false,
+      message:
+        error instanceof Error
+          ? error.message
+          : "Falha ao salvar os tokens de design"
@@ -1260 +1308 @@ figma.on("selectionchange", () => {
-            name: node.mainComponent?.name || 'Sem nome',
+            name: node.mainComponent?.name || "Sem nome",
@@ -1262 +1310 @@ figma.on("selectionchange", () => {
-            description: node.mainComponent?.description || ''
+            description: node.mainComponent?.description || ""
@@ -1264 +1312 @@ figma.on("selectionchange", () => {
-          
+
@@ -1268 +1316 @@ figma.on("selectionchange", () => {
-            
+
@@ -1270,2 +1318,9 @@ figma.on("selectionchange", () => {
-            for (const [key, prop] of Object.entries(node.componentProperties) as [string, any][]) {
-              if (!prop || prop.value === undefined || prop.value === null || prop.value === '') {
+            for (const [key, prop] of Object.entries(
+              node.componentProperties
+            ) as [string, any][]) {
+              if (
+                !prop ||
+                prop.value === undefined ||
+                prop.value === null ||
+                prop.value === ""
+              ) {
@@ -1274 +1329 @@ figma.on("selectionchange", () => {
-              
+
@@ -1276,2 +1331,2 @@ figma.on("selectionchange", () => {
-              const cleanKey = key.replace(/#[^:]+:\d+$/, '').trim();
-              
+              const cleanKey = key.replace(/#[^:]+:\d+$/, "").trim();
+
@@ -1280 +1335 @@ figma.on("selectionchange", () => {
-              if (typeof prop.value === 'boolean') {
+              if (typeof prop.value === "boolean") {
@@ -1282 +1337 @@ figma.on("selectionchange", () => {
-              } else if (typeof prop.value === 'string') {
+              } else if (typeof prop.value === "string") {
@@ -1284 +1339 @@ figma.on("selectionchange", () => {
-              } else if (typeof prop.value === 'object') {
+              } else if (typeof prop.value === "object") {
@@ -1289 +1344 @@ figma.on("selectionchange", () => {
-              
+
@@ -1293 +1348 @@ figma.on("selectionchange", () => {
-            
+
@@ -1296 +1351 @@ figma.on("selectionchange", () => {
-              name: node.mainComponent?.name || 'Sem nome',
+              name: node.mainComponent?.name || "Sem nome",
@@ -1298 +1353 @@ figma.on("selectionchange", () => {
-              description: node.mainComponent?.description || '',
+              description: node.mainComponent?.description || "",
