diff a/src/app/components/SettingsPanel.tsx b/src/app/components/SettingsPanel.tsx	(rejected hunks)
@@ -41 +41,4 @@ function SettingsPanel(props: SettingsPanelProps) {
-  const [saveStatus, setSaveStatus] = React.useState<{success: boolean; message: string} | null>(null);
+  const [saveStatus, setSaveStatus] = React.useState<{
+    success: boolean;
+    message: string;
+  } | null>(null);
@@ -55,4 +58 @@ function SettingsPanel(props: SettingsPanelProps) {
-      parent.postMessage(
-        { pluginMessage: { type: 'load-saved-tokens' } },
-        '*'
-      );
+      parent.postMessage({ pluginMessage: { type: "load-saved-tokens" } }, "*");
@@ -60 +60 @@ function SettingsPanel(props: SettingsPanelProps) {
-      console.error('Erro ao carregar tokens:', error);
+      console.error("Erro ao carregar tokens:", error);
@@ -63 +63,4 @@ function SettingsPanel(props: SettingsPanelProps) {
-        message: error instanceof Error ? error.message : 'Erro ao carregar tokens salvos'
+        message:
+          error instanceof Error
+            ? error.message
+            : "Erro ao carregar tokens salvos"
@@ -72 +75,4 @@ function SettingsPanel(props: SettingsPanelProps) {
-      setSaveStatus({ success: false, message: 'Nenhum token de design encontrado' });
+      setSaveStatus({
+        success: false,
+        message: "Nenhum token de design encontrado"
+      });
@@ -81,2 +87,7 @@ function SettingsPanel(props: SettingsPanelProps) {
-        { pluginMessage: { type: 'save-design-tokens', tokens: props.designTokens } },
-        '*'
+        {
+          pluginMessage: {
+            type: "save-design-tokens",
+            tokens: props.designTokens
+          }
+        },
+        "*"
@@ -85 +96 @@ function SettingsPanel(props: SettingsPanelProps) {
-      console.error('Erro ao salvar tokens:', error);
+      console.error("Erro ao salvar tokens:", error);
@@ -88 +99,2 @@ function SettingsPanel(props: SettingsPanelProps) {
-        message: error instanceof Error ? error.message : 'Erro ao salvar tokens'
+        message:
+          error instanceof Error ? error.message : "Erro ao salvar tokens"
@@ -103 +115 @@ function SettingsPanel(props: SettingsPanelProps) {
-      
+
@@ -106 +118 @@ function SettingsPanel(props: SettingsPanelProps) {
-      if (message.type === 'saved-tokens-loaded') {
+      if (message.type === "saved-tokens-loaded") {
@@ -112 +124,2 @@ function SettingsPanel(props: SettingsPanelProps) {
-            message: `Carregados ${message.tokens?.length || 0} conjuntos de tokens`
+            message: `Carregados ${message.tokens?.length ||
+              0} conjuntos de tokens`
@@ -117 +130 @@ function SettingsPanel(props: SettingsPanelProps) {
-            message: message.message || 'Falha ao carregar tokens salvos'
+            message: message.message || "Falha ao carregar tokens salvos"
@@ -120 +133 @@ function SettingsPanel(props: SettingsPanelProps) {
-      } else if (message.type === 'design-tokens-saved') {
+      } else if (message.type === "design-tokens-saved") {
@@ -124 +137,5 @@ function SettingsPanel(props: SettingsPanelProps) {
-          message: message.message || (message.success ? 'Tokens salvos com sucesso' : 'Falha ao salvar tokens')
+          message:
+            message.message ||
+            (message.success
+              ? "Tokens salvos com sucesso"
+              : "Falha ao salvar tokens")
@@ -132 +149 @@ function SettingsPanel(props: SettingsPanelProps) {
-    window.addEventListener('message', handleMessage);
+    window.addEventListener("message", handleMessage);
@@ -134 +151 @@ function SettingsPanel(props: SettingsPanelProps) {
-      window.removeEventListener('message', handleMessage);
+      window.removeEventListener("message", handleMessage);
@@ -136 +153 @@ function SettingsPanel(props: SettingsPanelProps) {
-  }, [])
+  }, []);
@@ -606 +623 @@ function SettingsPanel(props: SettingsPanelProps) {
-        await new Promise<void>((resolve) => {
+        await new Promise<void>(resolve => {
@@ -609,3 +626,5 @@ function SettingsPanel(props: SettingsPanelProps) {
-            if (pluginMessage && pluginMessage.type === 'design-tokens-saved') {
-              window.removeEventListener('message', handleSaveResponse);
-              console.log("[SettingsPanel] Tokens de design salvos com sucesso");
+            if (pluginMessage && pluginMessage.type === "design-tokens-saved") {
+              window.removeEventListener("message", handleSaveResponse);
+              console.log(
+                "[SettingsPanel] Tokens de design salvos com sucesso"
+              );
@@ -615,3 +634,3 @@ function SettingsPanel(props: SettingsPanelProps) {
-          
-          window.addEventListener('message', handleSaveResponse);
-          
+
+          window.addEventListener("message", handleSaveResponse);
+
@@ -619,2 +638,7 @@ function SettingsPanel(props: SettingsPanelProps) {
-            { pluginMessage: { type: 'save-design-tokens', tokens: props.designTokens } },
-            '*'
+            {
+              pluginMessage: {
+                type: "save-design-tokens",
+                tokens: props.designTokens
+              }
+            },
+            "*"
@@ -622 +646 @@ function SettingsPanel(props: SettingsPanelProps) {
-          
+
@@ -630 +653,0 @@ function SettingsPanel(props: SettingsPanelProps) {
-      
